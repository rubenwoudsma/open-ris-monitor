name: Publish

on:
  workflow_dispatch:

jobs:
  publish-placeholder:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Publish placeholder
        run: echo "Publishing is planned after the first harvest and public JSONL exports are available."
