name: Publish

on:
  workflow_dispatch:

jobs:
  publish:
    name: Publish placeholder
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Validate site folder exists
        run: test -d site

      - name: Explain current status
        run: echo "GitHub Pages publication will be implemented after public JSONL exports exist."
